UPDATE CODE

git status
git add . @ git file name
git commit -m 'message'
git push

GO BACK

git log --oneline

Option A (Just look at old code)
git checkout xxxxxxx

Option B (Undo last commit but keep the file)
git revert HEAD

Option C (Reset to a specific commit)
git reset --hard xxxxxxx
git push --force

##TO OPEN SERVER (DOCKER UPDATE)

Open Docker wait until Green

in terminal open wsl

To turn on SERVER
./vendor/bin/sail up -d

Open Browser
http://localhost
http://localhost:8080

To close SERVER
./vendor/bin/sail down

just in case

cd /mnt/c/xampp/htdocs/stress-system